{
  "name": "open-order",
  "version": "1.0.0",
  "private": true,
  "description": "An interactive smart-ordering concept for restaurant QR menus.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "test": "node --test",
    "validate": "node scripts/validate-catalog.mjs",
    "check": "npm run validate && npm test && npm run build",
    "preview": "npm run build && wrangler pages dev out",
    "deploy": "npm run check && wrangler pages deploy out --project-name open-order"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "next": "^16.2.10",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  },
  "overrides": {
    "postcss": "^8.5.10"
  }
}
